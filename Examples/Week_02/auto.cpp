int doit()
{
        auto i = 3;
        std::vector<std::vector<int>>::iterator it = v.begin();
        auto it = v.begin();
        for(auto &  row : v)
        {
                for (auto & col : row)
                {
                        row = 0;
                }
        }
}
