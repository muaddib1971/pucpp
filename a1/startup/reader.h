#include "list.h"
#include "tree.h"

namespace cpt323::reader
{
/* in this file, provide the the function prototypes for loading data into each
 * data structure */
#ifdef USELIST
#elif defined(USETREE)
#elif defined(USESTDLIST)
#elif defined(USESET)
#elif defined(USEVECTOR)
#endif
}
