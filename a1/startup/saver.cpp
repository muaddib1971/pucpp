#include "saver.h"
/* provide the implementation of the function to save each data structure here
 */
#ifdef USELIST
#elif defined(USETREE)
#elif defined(USESTDLIST)
#elif defined(USESET)
#elif defined(USEVECTOR)
#endif
