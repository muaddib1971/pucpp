#include "main.h"

int main(int argc, char* argv[])
{
/* start by parsing the command line arguments and output errors and exit if an
 * invalid command line is provided */

/* implement the code for each datastructure separately */
#ifdef USELIST
#elif defined(USETREE)
#elif defined(USESTDLIST)
#elif defined(USESET)
#elif defined(USEVECTOR)
#endif
}
