#include "reader.h"
/* provide the implementations for loading the data into each data structure in
 * this file */
#ifdef USELIST
#elif defined(USETREE)
#elif defined(USESTDLIST)
#elif defined(USESET)
#elif defined(USEVECTOR)
#endif
